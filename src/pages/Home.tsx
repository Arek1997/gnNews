import Response from '../components/response/Response';

const HomePage = () => {
	return (
		<Response
			text='Choose the country and start reading news!'
			classes='md:text-lg'
		/>
	);
};

export default HomePage;
